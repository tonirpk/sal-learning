const num = 8


function arrayFiller(x) {
    const arr = []
    for (let i = 0; i <= x; i++) {
        if (i <= x) {
            arr.push(i)
        }
    }
    return arr
}

console.log("The array is", arrayFiller(num))